version: '1'

workout:
  name: 'Understanding Trust'
  workout_description: 'Develop a web credential harvesting site to understand the dangers of misapplied trust'
  teacher_instructions_url:
  student_instructions_url: 'https://storage.googleapis.com/student_workout_instructions_tgd4419/TrustStudentInstructions.pdf'
  build_type: "compute"

student_entry:
  type: vnc
  password: "Grander Mus@sh!"
  network: external-network
  ip: 10.1.1.20

networks:
  - name: external-network
    subnets:
      - name: default
        ip_subnet: 10.1.1.0/24

servers:
  - name: cybergym-vnc
    image: image-cybergym-vnc
    machine_type: "n1-standard-1"
    nics:
      - network: external-network
        internal_IP: 10.1.1.20
        subnet: default
        external_NAT: True
  - name: kali
    image: kali-full-nessus-image
    machine_type: "n1-standard-1"
    nics:
      - network: external-network
        internal_IP: 10.1.1.30
        subnet: default
        external_NAT: False

# List all firewall rules. Multiple rules may be listed using YAML block sequence format.
firewall_rules:
  - name: allow-all-local-external
    network: external-network
    target_tags: []
    protocol: tcp
    ports: ["tcp/any", "udp/any", "icmp/any"]
    source_ranges: ["10.1.0.0/16"]
