version: '1'

workout:
  name: 'Networking Training Environment'
  workout_description: 'Network Training Environment'
  build_type: "compute"

student_entry:
  type: rdp
  security-mode: any
  username: forge
  password: "F0rg3up!4theWIN!"
  network: external-network
  ip: 10.0.0.2

networks: 
  - name: external-network
    subnets:
      - name: default
        ip_subnet: 10.0.0.0/24

servers:
  - name: networking
    image: networking-img
    machine_type: "n1-standard-1"
    nics:
      - network: external-network
        internal_IP: 10.0.0.2
        subnet: default
        external_NAT: False

# List all firewall rules. Multiple rules may be listed using YAML block sequence format.
firewall_rules:
  - name: allow-all-local-external
    network: external-network
    target_tags: []
    protocol: tcp
    ports: ["tcp/any", "udp/any", "icmp/any"]
    source_ranges: ["10.0.0.0/24"]
